pipeline{
  agent any
  stages{
    stage('ansible_start'){
      steps{
        git branch: 'main', url:
        ""
      }
    }
    stage('ansible_playbook'){
      steps{
        sh 'ansible-playbook playbook.yml' 
      }
    }
}
